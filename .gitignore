# ONNX 원본 (encoder는 279MB로 GitHub 제한 초과 — README의 다운로드 링크 참조)
zipformer/encoder-epoch-99-avg-1.onnx
zipformer/encoder-epoch-99-avg-1.int8.onnx

# 변환 완료 전 RKNN은 아직 없음 — 생성 후 커밋
# *.rknn  ← 주석 처리, 나중에 변환 후 올릴 것

# 데이터 (테스트 샘플 제외)
!zipformer/test_wavs/
*.wav
*.mp3
*.npz
calibration_data/
eval_result/
*.csv

# Python
__pycache__/
*.pyc
.env

# Claude Code
.claude/
